# OpsRelay UI

Start the dashboard locally:

```bash
npm install
npm run dev
```

Routes:
- / (Dashboard)
- /incidents/INC-1042 (Incident detail mock)
- /alerts
- /runbooks
- /connectors
- /chat

Chat:
- SSE mock endpoint at /api/chat/stream emits tool + assistant events.
